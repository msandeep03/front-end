import React, { Component } from 'react'


import Context              from './Context'


export default () => (WrappedComponent) => (props) => (
  <Context.Consumer>
    {(context) => (
      <WrappedComponent {...props} fromStage={context} />
    )}
  </Context.Consumer>
)