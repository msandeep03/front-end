import React, { Component } from 'react'
import { compose }          from 'recompose'
import styled               from 'styled-components'

import { Grid }             from '@material-ui/core'


import BlockScene           from '../block/BlockScene'


const Section = styled.div`
  width: 100vw;
  height: 600px;
`

const Grid100 = styled(Grid)`
  height: 100%;
`

const enhance = compose()

const SectionScene = enhance(({
  children,
}) => {
  return (
    <Section>
      <Grid100 container justify='center' spacing={24}>
        <Grid item xs={12}>
          <BlockScene />
        </Grid>
      </Grid100>
    </Section>
  )
})

export default SectionScene