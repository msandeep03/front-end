import axios                from 'axios'


export default async () => {
  const videoId = `2fkCsrI-Rz4`
  const uri = `http://www.youtube.com/get_video_info?video_id=${videoId}&el=embedded&ps=default&eurl=&gl=US&hl=en`
  const response = await axios.request({
    url: `https://gobetween.oklabs.org/pipe/${uri}`
  })

  console.log(response)
}