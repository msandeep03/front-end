import spaceConfig          from 'app/default/notify/space/spaceNotify'
import spaceInherit         from 'evoke-me/space/api/spaceInherit'

import config               from '../../config/api/config'


const { namespace } = config

export default spaceInherit(spaceConfig, { namespace })