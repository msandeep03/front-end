import spaceConfig          from 'app/default/nav/space/spaceNav'
import spaceInherit         from 'evoke-me/space/api/spaceInherit'

import spaceNotify          from '../../notify/space/spaceNotify'
import config               from '../../config/api/config'


const { namespace } = config

export default spaceInherit(spaceConfig, {
  dependencies: [
    spaceNotify,
  ],
  namespace,
})