import React, {Component} from 'react'
import {withStyles, Grid, Paper, ButtonBase} from '@material-ui/core';
import compose from 'recompose/compose'
import defaultProps from 'recompose/defaultProps'

import {
  Title, TagLine
} from '../../customUI';

import OccasionItem from '../OccasionItem'

const styles = theme => ({});

const enhance = compose(
  defaultProps({
    occasions: [
      {title: 'Weddingss', button: 'Explore', description: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit.'},
      {title: 'Babby Shower', button: 'Explore', description: 'Maecenas fringilla massa vitae nisl scelerisque lobortis.'},
      {title: 'Reception', button: 'Explore', description: 'Some other description. In case to see different text.'},
    ]
  })
)

const Occasions = enhance(({
  classes,
  occasions
}) => {
  return (
    <Grid container spacing={0} justify="center" alignItems="stretch" direction="row">
      <Grid item md={7} xs={12}>
        <Title size="1.875rem" color="#000" transform="uppercase" justify="center" margin="0 0 3.875rem 0">
          SPECIAL OCCASIONS
        </Title>
        <TagLine color="#000" size="2rem" fweight="300" margin="0 0 2.688rem 0">
          Lorem ipsum dolor sit amet, consectetur adipiscing elit. Maecenas fringilla massa vitae nisl scelerisque
          lobortis.
        </TagLine>
      </Grid>

      <Grid item xs={11} sm={11} md={10}>
        <Grid container alignItems="stretch" direction="row" justify="center" spacing={24}>
          {occasions.map((item, index) =>
            <OccasionItem key={index} {...item} bgurl={`/img/oc${index}.jpg`}></OccasionItem>
          )}
        </Grid>
      </Grid>
    </Grid>
  )
})
export default withStyles(styles)(Occasions)