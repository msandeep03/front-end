import React, {Component} from 'react'
import {Grid, ButtonBase, Typography, Button} from '@material-ui/core';
import compose from 'recompose/compose'
import {mediaGrid} from '../../customUI' 
import styled from 'styled-components';
import {rem} from 'polished'

const enhance = compose();


const ButtonPrimary = styled(({ width, ...other }) => (
  <Button {...other} />
))`
  && {
    background-color: #000;
    color: white;
    font-weight: 600;
    font-size: ${rem(20)};
    text-transform: uppercase;
    padding-top: ${rem(10)};
    padding-bottom: ${rem(10)};
   }
`;

const Root = styled(Grid)`
  && {
    text-align: center;
    height: ${rem(600)};

    ${mediaGrid(`xl`)}{//lg d
      height: ${rem(600)};
    }
    ${mediaGrid(`lg`)}{//sm d
      height: ${rem(600)};
    }
    ${mediaGrid(`md`)}{//tablet
      height: ${rem(500)};
    }
    ${mediaGrid(`sm`)}{//phone
      height: ${rem(400)};
    }
  }
`

const GridBg = styled(Grid)`
&& {
  background-position: center center;
  background-size: auto 100%;
  text-align: center;
  height: 38%;
  flex-wrap: nowrap;

  ${mediaGrid(`xl`)}{//lg d
    background-size: auto 100%;
    height: 40%;
  }
  ${mediaGrid(`lg`)}{//sm d
    background-size: auto 100%;
    height: 38%;
  }
  ${mediaGrid(`md`)}{//tablet
    background-size: auto 100%;
    height: 33%;
  }
  ${mediaGrid(`sm`)}{//phone
    background-size: auto 100%;
    height: 33%;
  }
}
`;

const Img = styled(ButtonBase)`
&& {

  width: ${rem(147)};
  height: auto
  

  img {
    width: ${rem(147)};
    height: auto
  }


  ${mediaGrid(`lg`)}{//sm d
    img {
      width: ${rem(147)};
      height: auto;
    }
  }


  ${mediaGrid(`md`)}{//tablet
    img {
      width: ${rem(99)};
      height: auto;
    }
  }
  ${mediaGrid(`sm`)}{//phone
    img {
      width: ${rem(67)};
      height: auto;
    }
  }
}
`;

const TagLine = styled(Typography)`
  && {
    font-size: ${rem(35)};
    font-weight: 300;
    color: #000;
    text-align: center;
    font-style: normal;
    line-height: normal; 

    ${mediaGrid(`md`)}{//tablet
      margin: ${rem(20)} auto;
    }
    ${mediaGrid(`sm`)}{//phone
      margin: ${rem(20)} auto;
    }
  }
`;

const Banner = enhance(() => {
  const url = '/img/jars.jpg'
  return (
    <Root container alignItems="center" direction="row" justify="space-between" >

      <GridBg item xs={12} sm={12} style={{height: '38%', backgroundImage: 'url('+url+')'}} ></GridBg>

      <Grid item xs={12} sm={12} >
        <Grid container direction={'column'} justify="center" alignItems="center" spacing={0}>
          <Grid xs={10} sm={8}  item>
            <TagLine>
              Lorem ipsum dolor sit amet, consectetur adipiscing elit. Maecenas fringilla massa vitae nisl scelerisque
              lobortis.
            </TagLine>
          </Grid>
        </Grid>
      </Grid>

      <Grid item xs={12} sm={12} >

      <Grid container direction={'column'} justify="center" alignItems="center" spacing={0}>
        <Grid xs={10} sm={10} item >
          <ButtonPrimary fullWidth variant="contained" color="primary">Explore our shop</ButtonPrimary>
        </Grid>
      </Grid>

      </Grid>

    </Root>
  )
})

export default Banner