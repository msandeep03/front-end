import React, {Component} from 'react'
import {Grid, ButtonBase, Typography} from '@material-ui/core';
import compose from 'recompose/compose'
import {mediaGrid} from '../../customUI' 
import Poster from '../Poster'
import {
  ButtonPrimary,
  BannerStretch
} from '../../customUI.js';
import styled from 'styled-components';
import {rem} from 'polished'

const enhance = compose();


const Root = styled(Grid)`
&& {
  background-position: center center;
  background-repeat: no-repeat
  background-color: #fff;
  background-size: 100% auto;
  background-image: url('/img/jars.jpg')
  text-align: center;
  height: ${rem(600)};

  ${mediaGrid(`xl`)}{//lg d
    height: ${rem(600)};
  }
  ${mediaGrid(`lg`)}{//sm d
    height: ${rem(600)};
  }
  ${mediaGrid(`md`)}{//tablet
    height: ${rem(500)};
  }
  ${mediaGrid(`sm`)}{//phone
    height: ${rem(400)};
  }
}
`

const WrapBanner = styled(Grid)`
&& {

  height: ${rem(248)};
  overflow: hidden;
  flex-wrap: nowrap;

  ${mediaGrid(`xl`)}{//lg d
    height: ${rem(248)};
  }
  ${mediaGrid(`lg`)}{//sm d
    height: ${rem(248)};
  }
  ${mediaGrid(`md`)}{//tablet
    height: ${rem(168)};
  }
  ${mediaGrid(`sm`)}{//phone
    height: ${rem(118)};
  }
}
`;

const Img = styled(ButtonBase)`
&& {

  width: ${rem(147)};
  height: auto
  

  img {
    width: ${rem(147)};
    height: auto
  }


  ${mediaGrid(`lg`)}{//sm d
    img {
      width: ${rem(147)};
      height: auto;
    }
  }


  ${mediaGrid(`md`)}{//tablet
    img {
      width: ${rem(99)};
      height: auto;
    }
  }
  ${mediaGrid(`sm`)}{//phone
    img {
      width: ${rem(67)};
      height: auto;
    }
  }
}
`;

const TagLine = styled(Typography)`
  && {
    font-size: ${rem(35)};
    font-weight: 300;
    color: #000;
    text-align: center;
    font-style: normal;
    line-height: normal; 
  }
`;

const Banner = enhance(() => {
  return (
    <Root container alignItems="stretch" direction="column" justify="center" style={{background:'red'}}>

      <Grid item>
        <BannerStretch >
          <WrapBanner container alignItems="stretch" justify="center" spacing={0} >
            {(Array.apply(null, Array(20))).map((value, index) => (
              
              <Grid key={index} item  >
                <Img/>
              </Grid>
            ))}
          </WrapBanner>
        </BannerStretch>
      </Grid>


      <Grid item xs={10} sm={8} className={'wrapper no-bg'}>
        <TagLine>
          Lorem ipsum dolor sit amet, consectetur adipiscing elit. Maecenas fringilla massa vitae nisl scelerisque
          lobortis.
        </TagLine>
      </Grid>

      <Grid item>
        <Grid container direction={'row'} justify="center" alignItems="center" spacing={0}>
          <Grid xs={10} sm={4} item>
            <ButtonPrimary fullWidth variant="contained" color="primary">Explore our shop</ButtonPrimary>
          </Grid>
        </Grid>
      </Grid>

    </Root>
  )
})

export default Banner