import React, {Component} from 'react'
import {Grid, Typography, FormControl} from '@material-ui/core';
import compose from 'recompose/compose'
import styled from 'styled-components'
import {rem} from 'polished'
import {mediaGrid, ButtonPrimary} from '../../customUI'


const ButtonBaseImg = styled(FormControl)`
  && {
    text-align: center;
    margin-right: ${rem(40)};
    width: ${rem(100)};

    img {
      width: ${rem(100)};
      height: auto;
    }

    ${mediaGrid(`md`)}{//tablet
      text-align: left;
        img {
          width: ${rem(100)};
          height: auto;
        }
      }
    ${mediaGrid(`sm`)}{//phone
      text-align: center;
      img {
        width: ${rem(100)};
        height: auto;
      }
    }
  }
`;


const ProductParam = styled(Typography)`
  && {
    font-size:  ${rem(16)};
    line-height:  ${rem(23)};
    font-style: normal;
    font-stretch: normal;
    line-height: normal;
    letter-spacing: normal; 
    margin-bottom: ${rem(5)};
  }
`;


const Title = styled(Typography)`
  && {
    font-weight: 700;
    font-size: ${rem(30)};
    line-height: normal;
    border-bottom: 1px solid;
    padding-bottom: ${rem(7)};
    margin-bottom: ${rem(13)};
    color: rgba(0, 0, 0, 0.87);
    border-color: rgba(0, 0, 0, 0.87);
    text-transform: uppercase;
  }
`

const SubTitle = styled(Typography)`
  && {
    font-size: ${rem(16)};
    margin-bottom: ${rem(22)};
  }
`

const enhance = compose()

const ProductSampleList = enhance(({}) => {
  return (

  
    <Grid container direction={'row'} justify={'flex-start'} alignItems={'center'} spacing={0}>
      <Grid item >

        <ButtonBaseImg>
          <img alt={'productTitle'} src="/img/product1.jpg"/>
        </ButtonBaseImg>

      </Grid>

      <Grid item >

          <FormControl className={'details'}>
            <Grid container direction={'column'} alignItems={'stretch'} justify={'space-around'} spacing={0}>
                <Grid item xs>
                  <Title>
                    Loquat and wild flower
                  </Title>
                  <SubTitle variant="caption">
                    An aromatic winter honey from the Alman Plateau
                  </SubTitle>

                  <ProductParam paragraph={true}>
                    <b>Sweetness:</b> 3/5
                  </ProductParam>
                  <ProductParam paragraph={true}>
                    <b>Taste:</b> zest, fruit, nut and spice notes
                  </ProductParam>
                  <ProductParam paragraph={true}>
                    <b>Food pairing:</b> breads, dairy products and desserts
                  </ProductParam>
                  <ProductParam paragraph={true}>
                    <b>Texture:</b> small to fine crystals
                  </ProductParam>

                </Grid>
                <Grid item xs>
                  <br/>
                  <ButtonPrimary fullWidth variant="contained" color="primary">Buy Now</ButtonPrimary>
                </Grid>
            </Grid>

          </FormControl>

      </Grid>
    </Grid>


    )
})

export default ProductSampleList