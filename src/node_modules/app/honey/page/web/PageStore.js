import React from 'react'
import theme from 'app/honey/page/web/themeMUI';
import { Grid } from '@material-ui/core';
import Page from 'evoke-me/page/web/Page'
import Header from 'app/honey/page/web/components/Header'
import SubHeader from 'app/honey/page/web/components/SubHeader'
import Poster from 'app/honey/page/web/components/Poster'
import SocialThumbs from 'app/honey/page/web/components/SocialThumbs'
import ProductSample from 'app/honey/page/web/components/ProductSample'
import Subscribe from 'app/honey/page/web/components/Subscribe'
import Apply from 'app/honey/page/web/components/Apply'
import PressQuotes from 'app/honey/page/web/components/PressQuotes'
import Testimonial from 'app/honey/page/web/components/Testimonial'
import Occasions from 'app/honey/page/web/components/Occasions'
import Banner from 'app/honey/page/web/components/Banner'
import Footer from 'app/honey/page/web/components/Footer'
import Stripe from "app/honey/page/web/components/Stripe";
import styled from 'styled-components'
import { StickyContainer, Sticky } from 'react-sticky';
import {
  CssBaseline,
  MuiThemeProvider,
  Typography
} from '@material-ui/core';

import {rem} from 'polished'

const Title = styled(Typography)`&& {
  color: ${props => props.textColor || '#fff'};
  margin: ${props => props.margin};
  font-weight: ${props => props.fweight || '700'};
  min-width: auto;
  width: 100%;
  font-size: ${rem(35)};
  text-align: center;
  font-size: ${rem(38)};
  text-transform: uppercase;

}
`;


const ReadMore = styled(({ size, align, margin, bold, color, ...other }) => (
  <Typography {...other} />
))`
&& {
  text-align: center;
  color: ${props => props.color || '#fff'};
  font-size: ${props => props.size};
  line-height: 1.4;
  font-weight: ${props => props.bold || 'normal'};
  font-style: italic;
  margin: ${props => props.margin || '0'};
  padding: ${props => props.padding || '0'};

  a {
    text-decoration: underline;
    color: ${props => props.color || '#fff'};
    font-weight: ${props => props.bold || 'normal'};
    &:hover {
      text-decoration: none;
    }
  }

}
`;

const TagLineContainer = styled.div`
&& {
  text-align: center;
  &.dark {
    padding: ${rem(70)} ${rem(45)};
    background-color: rgba(0, 0, 0, 0.2);
  }
}
`;

const TagLine = styled(({ color, transform, border, fweight, ...other }) => (
  <Typography {...other} />
))`
&& {
  font-size: ${rem(35)};
  font-weight: ${props => props.fweight || 400};
  color: ${props => props.color};
  text-align: center;
  font-style: normal;
  line-height: normal; 
  &.dark {
    padding: ${rem(70)} ${rem(45)};
    background-color: rgba(0, 0, 0, 0.2);

  }
}
`;

const SubHeaderWrapper = styled.div`
&& {
  z-index: 10;
  display: block;

}
`; 

const PageStore  = () => {
  return (
    <MuiThemeProvider theme={theme}>
      <CssBaseline />
      {/* CssBaseline is normalization for styles */}

      <Page hasFooter={true}>
        {/* Header */}

          <StickyContainer>   

            <Sticky>
              {({style}) => (
                <SubHeaderWrapper style={style} ><SubHeader /></SubHeaderWrapper>
                )}
            </Sticky>


           PageStore  PageStore  PageStore  store
           PageStore  PageStore  PageStore  store
           PageStore  PageStore  PageStore  store
           PageStore  PageStore  PageStore  store


          </StickyContainer>
  
          {/* footer */}
          <Footer></Footer>


        </Page>
    </MuiThemeProvider>  
  )
}
export default PageStore